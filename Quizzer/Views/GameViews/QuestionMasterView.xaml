<base:WindowBase x:Class="Quizzer.Views.GameViews.QuestionMasterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Quizzer.Views.GameViews"
        xmlns:base="clr-namespace:Quizzer.Base"
        xmlns:questions="clr-namespace:Quizzer.Views.GameViews.QuestionViews"
        xmlns:buzzerServer="clr-namespace:Quizzer.Views.BuzzerViews"
        xmlns:typed="clr-namespace:Quizzer.Views.GameViews.QuestionViews.Typed"
        mc:Ignorable="d"
        Title="QuestionMasterView" Height="auto" Width="auto">
    <base:WindowBase.DataContext>
        <local:CurrentQuestionViewModel></local:CurrentQuestionViewModel>
    </base:WindowBase.DataContext>
    <Window.Resources>
        <questions:QuestionStepTemplateSelector x:Key="QuestionStepTemplateSelector" />

        <DataTemplate x:Key="DefaultStepTemplate">
            <typed:DefaultStepView IsMasterView="True" />
        </DataTemplate>

        <DataTemplate x:Key="MultipleChoiceStepTemplate">
            <typed:MultipleChoiceStepView IsMasterView="True" />
        </DataTemplate>
    </Window.Resources>
    <Grid Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="1" Grid.Column="0">
            <questions:CurrentQuestionView ></questions:CurrentQuestionView>
        </StackPanel>

        <Grid Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" MinWidth="200" ClipToBounds="True">
            <Grid.Background>
                <ImageBrush ImageSource="{Binding BackgroundImagePath}"
                     Stretch="UniformToFill" />
            </Grid.Background>

            <Grid.RowDefinitions>
                <RowDefinition Height="100" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Grid.Column="0" Background="Black" MaxHeight="100" ClipToBounds="True">
                <Viewbox Margin="5" Stretch="Uniform">
                    <TextBlock Text="{Binding Question.QuestionText}"
                     Foreground="WhiteSmoke"
                     FontWeight="SemiBold"
                     TextWrapping="Wrap"
                     TextAlignment="Left"
                     VerticalAlignment="Center"
                     HorizontalAlignment="Center" />
                </Viewbox>
            </Grid>

            <Grid  Grid.Row="1" Grid.Column="0" MinWidth="200" ClipToBounds="True">
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <Label Grid.Row="0" Content="Current Step" Foreground="DarkGreen" FontWeight="Bold" FontSize="14" />
                <ContentControl Grid.Row="1"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Content="{Binding CurrentStepContext}"
                    ContentTemplateSelector="{StaticResource QuestionStepTemplateSelector}" />
            </Grid>

            <Grid  Grid.Row="2" Grid.Column="0" MinWidth="200"  ClipToBounds="True">
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <Label Grid.Row="0" Content="Next Step" Foreground="DarkBlue" FontWeight="Bold" FontSize="14" />
                <ContentControl Grid.Row="1"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Content="{Binding NextStepContext}"
                    ContentTemplateSelector="{StaticResource QuestionStepTemplateSelector}" />
            </Grid>

            <Grid Grid.Row="3" Grid.Column="0">
                <Border BorderBrush="WhiteSmoke">
                    <buzzerServer:BuzzerControlsView Background="{Binding BackgroundBrush}"></buzzerServer:BuzzerControlsView>
                </Border>
            </Grid>

            <Grid  Grid.Row="4" Grid.Column="0" MinWidth="200" ClipToBounds="True">
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <Label Grid.Row="0" Content="Finish Step" Foreground="DarkRed" FontWeight="Bold" FontSize="14" />
                <ContentControl Grid.Row="1"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Content="{Binding FinishStepContext}"
                    ContentTemplateSelector="{StaticResource QuestionStepTemplateSelector}" />
            </Grid>
        </Grid>
    </Grid>
</base:WindowBase>