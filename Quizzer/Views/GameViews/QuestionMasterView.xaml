<base:WindowBase x:Class="Quizzer.Views.GameViews.QuestionMasterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Quizzer.Views.GameViews"
        xmlns:base="clr-namespace:Quizzer.Base"
        xmlns:questions="clr-namespace:Quizzer.Views.GameViews.QuestionViews"
        xmlns:buzzerServer="clr-namespace:Quizzer.Views.BuzzerViews"
        xmlns:typed="clr-namespace:Quizzer.Views.GameViews.QuestionViews.Typed"
        mc:Ignorable="d"
        Title="QuestionMasterView" Height="auto" Width="auto">
    <base:WindowBase.DataContext>
        <local:CurrentQuestionViewModel></local:CurrentQuestionViewModel>
    </base:WindowBase.DataContext>
    <Window.Resources>
        <questions:QuestionStepTemplateSelector x:Key="QuestionStepTemplateSelector" />

        <DataTemplate x:Key="DefaultStepTemplate">
            <typed:DefaultStepView />
        </DataTemplate>

        <DataTemplate x:Key="MultipleChoiceStepTemplate">
            <typed:MultipleChoiceStepView />
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0" Grid.Column="1" Background="AliceBlue">
            <Viewbox Margin="5" Stretch="Uniform">
                <TextBlock Text="{Binding Question.QuestionText}"
                   FontWeight="SemiBold"
                   TextWrapping="Wrap"
                   TextAlignment="Left"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center" />
            </Viewbox>
        </Grid>

        <ScrollViewer Grid.Row="0" Grid.Column="0" Grid.RowSpan="2">
            <questions:CurrentQuestionView ></questions:CurrentQuestionView>
        </ScrollViewer>
        <buzzerServer:BuzzerControlsView Grid.Row="2" Grid.Column="0" Background="{Binding BackgroundBrush}"></buzzerServer:BuzzerControlsView>

        <Grid Grid.Row="1" Grid.Column="1" Grid.RowSpan="2" MinWidth="200" Background="Wheat">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Grid.Column="0" MinWidth="200" Background="Wheat">
                <Label Content="Current Step" Foreground="DarkGreen" FontWeight="Bold" FontSize="14" />
                <ContentControl Content="{Binding CurrentStepContext}"
                    ContentTemplateSelector="{StaticResource QuestionStepTemplateSelector}" />
            </StackPanel>

            <StackPanel Grid.Row="1" Grid.Column="0" MinWidth="200" Background="Wheat">
                <Label Content="Next Step" Foreground="DarkBlue" FontWeight="Bold" FontSize="14" />
                <ContentControl Content="{Binding NextStepContext}"
                    ContentTemplateSelector="{StaticResource QuestionStepTemplateSelector}" />
            </StackPanel>
        </Grid>
    </Grid>
</base:WindowBase>