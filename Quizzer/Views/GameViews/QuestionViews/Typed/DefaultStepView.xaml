<local:UserControlStepViewBase x:Class="Quizzer.Views.GameViews.QuestionViews.Typed.DefaultStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Quizzer.Views.GameViews.QuestionViews.Typed"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid ClipToBounds="True">
        <!-- shown when DataContext != null -->
        <Grid ClipToBounds="True">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <!-- linke Seite -->
            <Grid Grid.Column="0" Margin="4" ClipToBounds="True">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" x:Name="StackPanleIsResult">
                    <Label Content="Result:" />
                    <CheckBox VerticalContentAlignment="Center"
                              IsEnabled="False"
                              IsChecked="{Binding Step.IsResult}" />
                </StackPanel>

                <StackPanel Grid.Row="1" Orientation="Horizontal" x:Name="StackPanleIsFinish">
                    <Label Content="Finish:" />
                    <CheckBox VerticalContentAlignment="Center"
                              IsEnabled="False"
                              IsChecked="{Binding Step.IsFinish}" />
                </StackPanel>

                <Grid Grid.Row="2" ClipToBounds="True">
                    <Viewbox Stretch="Uniform"
                             StretchDirection="Both"
                             HorizontalAlignment="Stretch"
                             VerticalAlignment="Stretch">
                        <TextBlock Text="{Binding Step.StepText}"
                                   Foreground="WhiteSmoke"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center" />
                    </Viewbox>
                </Grid>
            </Grid>

            <!-- rechte Seite -->
            <Grid Grid.Column="1" Margin="4" ClipToBounds="True">
                <local:ResourceViewerControl
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    DataContext="{Binding Step}"
                    RootFolder="{Binding DataContext.ResourceRootFolder, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    IsMasterView="{Binding IsMasterView, RelativeSource={RelativeSource AncestorType=local:DefaultStepView}}"
                    AudioPlaceholderFile="{Binding DataContext.AudioPlaceholderFile, RelativeSource={RelativeSource AncestorType=UserControl}}" />
            </Grid>
        </Grid>

        <!-- shown when DataContext == null -->
        <Grid>
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>

            <StackPanel>
                <Label Content="Empty" Foreground="DarkRed" />
            </StackPanel>
        </Grid>
    </Grid>
</local:UserControlStepViewBase>