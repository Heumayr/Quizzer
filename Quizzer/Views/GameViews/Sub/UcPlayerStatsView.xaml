<UserControl x:Class="Quizzer.Views.GameViews.Sub.UcPlayerStatsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Quizzer.Views.GameViews.Sub"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="340">
    <!--<UserControl.DataContext>
        <local:PlayerStatsContext></local:PlayerStatsContext>
    </UserControl.DataContext>-->
    <Border BorderThickness="2" BorderBrush="Transparent" CornerRadius="2" Background="{Binding BackgroundBrush}" Padding="15">
        <Grid MinHeight="50" MinWidth="50" Background="Transparent" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="auto"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Grid.Column="0" HorizontalAlignment="Center">
                <TextBlock FontSize="18" Foreground="Wheat"  FontWeight="ExtraBlack" Text="{Binding Player.Designation}"></TextBlock>
            </Grid>

            <Grid Grid.Row="1"  Grid.Column="0" HorizontalAlignment="Center">
                <TextBlock FontSize="18" Foreground="Wheat"  FontWeight="DemiBold" Text="{Binding Player.DisplayName}"></TextBlock>
            </Grid>

            <Border Grid.Row="2" Margin="5" Grid.Column="0" Width="80" Height="80" BorderThickness="3" CornerRadius="10" BorderBrush="#333333">
                <Border.Background>
                    <ImageBrush ImageSource="{Binding PlayerImagePath}"
                     Stretch="Uniform" />
                </Border.Background>
            </Border>

            <Grid Grid.Row="3" Grid.Column="0" x:Name="ScoreGrid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"></RowDefinition>
                </Grid.RowDefinitions>

                <TextBlock Grid.Column="0" Grid.Row="0" Foreground="Wheat" FontWeight="ExtraBold" FontSize="20" Text="Score: "></TextBlock>
                <TextBlock Grid.Column="1" Grid.Row="0" Foreground="Wheat" FontWeight="ExtraBold" FontSize="20" Text="{Binding ActualScore, Mode=OneWay}"  HorizontalAlignment="Left"></TextBlock>
            </Grid>
        </Grid>
    </Border>
</UserControl>