<UserControl x:Class="Quizzer.Views.GameViews.Sub.UcPlayerResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Quizzer.Views.GameViews.Sub"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <!--<UserControl.DataContext>
        <local:PlayerResultContext></local:PlayerResultContext>
    </UserControl.DataContext>-->
    <Grid MinHeight="50" MinWidth="50" Background="{Binding BackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="{Binding Player.Designation}"></Label>
        <Label Grid.Row="1" Grid.Column="0"  Content="{Binding Player.DisplayName}"></Label>

        <Grid Grid.Row="2" Grid.Column="0" Width="150" Height="150">
            <Grid.Background>
                <ImageBrush ImageSource="{Binding PlayerImagePath}"
                   Stretch="UniformToFill" />
            </Grid.Background>
        </Grid>

        <CheckBox Grid.Row="3" Grid.Column="0" IsEnabled="False" IsChecked="{Binding IsBuzzerWinner, Mode=OneWay}"></CheckBox>
    </Grid>
</UserControl>