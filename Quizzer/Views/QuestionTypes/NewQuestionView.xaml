<base:WindowBase x:Class="Quizzer.Views.QuestionTypes.NewQuestionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Quizzer.Views.QuestionTypes"
        xmlns:base="clr-namespace:Quizzer.Base"
        mc:Ignorable="d"
        Title="Neue Frage" Height="520" Width="620"
        WindowStartupLocation="CenterOwner">
    <base:WindowBase.DataContext>
        <local:NewQuestionViewModel />
    </base:WindowBase.DataContext>

    <DockPanel Background="{StaticResource AppBgBrush}">

        <Border DockPanel.Dock="Top" Background="{StaticResource PanelBgBrush}"
                BorderThickness="0,0,0,1" Padding="12,10">
            <StackPanel>
                <TextBlock Text="Welche Art von Frage?" FontSize="18" FontWeight="Bold" />
                <TextBlock Opacity="0.75" TextWrapping="Wrap" Margin="0,4,0,0"
                           Text="Der Typ bestimmt, wie die Spieler antworten und welche Felder danach ausgefüllt werden. Er lässt sich später nur über Umwandeln ändern." />
            </StackPanel>
        </Border>

        <Border DockPanel.Dock="Bottom" Background="{StaticResource PanelBgBrush}"
                BorderThickness="0,1,0,0" Padding="12,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Abbrechen"
                        ToolTip="Legt keine Frage an." Command="{Binding CancelCommand}"
                        MinWidth="110" Margin="0,0,8,0" />
                <Button Content="Anlegen"
                        ToolTip="Legt die Frage im gewählten Typ an und öffnet sie im Editor." Command="{Binding CreateCommand}" MinWidth="130" />
            </StackPanel>
        </Border>

        <ListBox ItemsSource="{Binding Profiles}"
                 SelectedItem="{Binding SelectedProfile, Mode=TwoWay}"
                 Margin="10" BorderThickness="0"
                 HorizontalContentAlignment="Stretch"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="10,8" Margin="0,2">
                        <StackPanel>
                            <TextBlock Text="{Binding DisplayName}" FontSize="15" FontWeight="DemiBold" />
                            <TextBlock Text="{Binding HelpText}" TextWrapping="Wrap"
                                       Opacity="0.75" Margin="0,3,0,0" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
</base:WindowBase>
