<UserControl x:Class="Quizzer.Views.HelperViewModels.UcCellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:enums="clr-namespace:Quizzer.DataModels.Enumerations;assembly=Quizzer.DataModels"
             x:Name="root">

    <UserControl.Resources>
        <Style x:Key="CellBorderStyle" TargetType="Border">
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="Padding" Value="3" />
            <Setter Property="Margin" Value="1" />
            <Setter Property="Background" Value="{Binding IsDoneBrush}" />
            <Setter Property="BorderBrush" Value="Gray" />

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="DarkGreen" />
                    <Setter Property="BorderBrush" Value="Black" />
                    <Setter Property="BorderThickness" Value="1" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Border Style="{StaticResource CellBorderStyle}">
        <Border.InputBindings>
            <MouseBinding MouseAction="LeftClick"
                       Command="{Binding ElementName=root, Path=Command}"
                       CommandParameter="{Binding ElementName=root, Path=CommandParameter}" />
        </Border.InputBindings>

        <StackPanel>
            <StackPanel.Resources>
                <Style TargetType="Label">
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0" />
                </Style>
            </StackPanel.Resources>

            <Label Foreground="{Binding IsDoneTextBrush}" Content="{Binding DisplayCoords}" />
            <Label Foreground="{Binding IsDoneTextBrush}" Content="{Binding DisplayCoordsFull}" />
            <Label Foreground="{Binding IsDoneTextBrush}" Content="{Binding DisplayText}" />
            <Label Foreground="{Binding IsDoneTextBrush}" Content="{Binding DifficultyDisplay}" />
            <Label Foreground="{Binding IsDoneTextBrush}" Content="{Binding PointsDisplay}" />
        </StackPanel>
    </Border>
</UserControl>