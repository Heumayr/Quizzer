<base:WindowBase x:Class="Quizzer.Views.HelperViewModels.QuestionSelectorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Quizzer.Views.HelperViewModels"
        xmlns:base="clr-namespace:Quizzer.Base"
        mc:Ignorable="d"
        Title="QuestionSelectorView" Height="450" Width="800">
    <base:WindowBase.DataContext>
        <local:QuestionSelectorViewModel />
    </base:WindowBase.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Margin="5">
            <Button Content="Close" Width="120" Height="20" Margin="0,0,0,10" Command="{Binding CloseCommand}" />
        </StackPanel>

        <StackPanel Grid.Column="1">
            <StackPanel Orientation="Horizontal" Margin="5">
                <Button Content="New" Width="100" Height="20" Margin="0,0,0,0" Command="{Binding AddQuestionCommand}" />
                <Button Content="Edit Selected" Width="100" Height="20" Margin="10,0,0,0" Command="{Binding EditSelectedQuestionCommand}" />
                <Label MinWidth="120" Margin="10,0,0,0" Content="Selected Question:" />
                <Label Content="{Binding Path=CurrentSelectedQuestionDisplay}" MinWidth="200" Margin="5,0,0,0" />
                <Button Content="Deselect" Width="100" Height="20" Margin="10,0,0,0" Command="{Binding DeselectCommand}" />
            </StackPanel>
            <DataGrid ItemsSource="{Binding AvailableQuestions}" AutoGenerateColumns="False" Margin="5" CanUserAddRows="False" SelectionMode="Single" IsReadOnly="True" SelectedItem="{Binding SelectedQuestion}"
                        base:DataGridRowDoubleClickBehavior.DoubleClickCommand="{Binding SelectAndCloseCommand}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Designation" Binding="{Binding Designation}" Width="2*" />
                    <DataGridTextColumn Header="Category" Binding="{Binding Category.Designation}" Width="*" />
                    <DataGridTextColumn Header="Difficulty" Binding="{Binding Difficulty}" Width="*" />
                    <DataGridTextColumn Header="Points" Binding="{Binding Points}" Width="*" />
                    <DataGridTextColumn Header="Minus-Points" Binding="{Binding MinusPoints}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</base:WindowBase>