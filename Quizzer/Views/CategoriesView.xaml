<base:WindowBase x:Class="Quizzer.Views.CategoriesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:base="clr-namespace:Quizzer.Base"
        xmlns:local="clr-namespace:Quizzer.Views"
        mc:Ignorable="d"
        Title="Kategorien" Height="450" Width="800">
    <base:WindowBase.DataContext>
        <local:CategoriesViewModel />
    </base:WindowBase.DataContext>
    <Grid Background="Black">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Margin="5">
            <Button Content="Speichern" Command="{Binding SaveCommand}" Style="{StaticResource DangerButton}" />
        </StackPanel>

        <DataGrid Grid.Column="1" ItemsSource="{Binding Categories}" AutoGenerateColumns="False" Margin="5" CanUserAddRows="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Bezeichnung" Binding="{Binding Designation}" Width="2*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</base:WindowBase>