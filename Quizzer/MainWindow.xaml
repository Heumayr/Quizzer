<base:WindowBase x:Class="Quizzer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Quizzer"
        xmlns:vm="clr-namespace:Quizzer.Views"
        xmlns:base="clr-namespace:Quizzer.Base"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <base:WindowBase.DataContext>
        <vm:MainViewModel />
    </base:WindowBase.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Margin="5">
            <Button Content="Start Quiz" Width="120" Height="20" Margin="0,0,0,10" Command="{Binding StartQuizCommand}" />
            <Button Content="Categories" Width="120" Height="20" Margin="0,0,0,10" Command="{Binding CategoryCommand}" />
            <Button Content="Questions" Width="120" Height="20" Margin="0,0,0,10" Command="{Binding QuestionsCommand}" />
            <Button Content="Players" Width="120" Height="20" Margin="0,0,0,10" Command="{Binding PlayersCommand}" />
        </StackPanel>
    </Grid>
</base:WindowBase>