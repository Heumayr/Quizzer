<UserControl x:Class="Quizzer.Views.GameViews.QuestionViews.CurrentQuestionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Quizzer.Views.GameViews.QuestionViews"
             xmlns:base="clr-namespace:Quizzer.Base"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800" Background="AliceBlue">
    <UserControl.Resources>
        <base:EqualityMultiConverter x:Key="EqualityMultiConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label MinWidth="120" Content="Type:" VerticalAlignment="Center" />
                <TextBox IsReadOnly="True" VerticalContentAlignment="Center" Text="{Binding Path=QuestionType, Mode=OneWay}" MinWidth="200" Margin="5,0,0,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label MinWidth="120" Content="Designation:" VerticalAlignment="Center" />
                <TextBox IsReadOnly="True" VerticalContentAlignment="Center" Text="{Binding Path=QuestionDesignation, Mode=OneWay}" MinWidth="200" Margin="5,0,0,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label MinWidth="120" Content="Designation short:" VerticalAlignment="Center" />
                <TextBox IsReadOnly="True" VerticalContentAlignment="Center" Text="{Binding Path=QuestionDesignationShort, Mode=OneWay}" MinWidth="200" Margin="5,0,0,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label MinWidth="120"  Content="Category:" VerticalAlignment="Center" />
                <TextBox IsReadOnly="True" VerticalContentAlignment="Center" Text="{Binding Path=QuestionCategory, Mode=OneWay}" MinWidth="200" Margin="5,0,0,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label MinWidth="120"  Content="Difficulty:" VerticalAlignment="Center" />
                <TextBox IsReadOnly="True" VerticalContentAlignment="Center" Text="{Binding Path=QuestionDifficulty, Mode=OneWay}" MinWidth="200" Margin="5,0,0,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label MinWidth="120" Content="Phase:" VerticalAlignment="Center" />
                <TextBox IsReadOnly="True" VerticalContentAlignment="Center" Text="{Binding Phase, Mode=OneWay}" MinWidth="200" Margin="5,0,0,0"></TextBox>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label MinWidth="120" Content="Points:" VerticalAlignment="Center" />
                <TextBox IsReadOnly="True" VerticalContentAlignment="Center" Text="{Binding CurrentPoints, Mode=OneWay}" MinWidth="200" Margin="5,0,0,0"></TextBox>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label MinWidth="120" Content="Minus-Points:" VerticalAlignment="Center" />
                <TextBox IsReadOnly="True" VerticalContentAlignment="Center" Text="{Binding CurrentMinusPoints, Mode=OneWay}" MinWidth="200" Margin="5,0,0,0"></TextBox>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="5">
                <Label MinWidth="120" Content="Notes:" VerticalAlignment="Center" />
                <TextBox Grid.Column="1"
               Text="{Binding QuestionNotes, Mode=OneWay}"
               Margin="5,0,0,0"
               AcceptsReturn="True"
               TextWrapping="Wrap"
               VerticalScrollBarVisibility="Auto"
               MinWidth="200"
               MinHeight="60"
               MaxHeight="200"
               HorizontalAlignment="Stretch"
               VerticalAlignment="Top" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="5,10,0,0">
                <Button Content="Start" Command="{Binding StartStepCommnad}" />
                <Button Content="Next" Command="{Binding NextStepCommnad}" />
                <Button Content="Back" Command="{Binding BackStepCommnad}" />
            </StackPanel>
            <DataGrid ItemsSource="{Binding QuestionSteps}"
          AutoGenerateColumns="False"
          Margin="5"
          CanUserAddRows="False"
          CanUserResizeColumns="True"
          CanUserReorderColumns="True"
          CanUserSortColumns="False"
          IsReadOnly="True"
          base:DataGridSelectedItemsBehavior.SelectedItems="{Binding SelectedSteps}"
          base:DataGridRowDoubleClickBehavior.DoubleClickCommand="{Binding OpenStepCommand}">

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <DataTrigger Value="True">
                                <DataTrigger.Binding>
                                    <MultiBinding Converter="{StaticResource EqualityMultiConverter}">
                                        <!-- current row Id -->
                                        <Binding Path="Id" />
                                        <!-- CurrentStep.Id from the DataGrid DataContext -->
                                        <Binding Path="DataContext.CurrentStep.Id"
                                     RelativeSource="{RelativeSource AncestorType=DataGrid}" />
                                    </MultiBinding>
                                </DataTrigger.Binding>

                                <Setter Property="Background" Value="DarkGreen" />
                                <Setter Property="Foreground" Value="White" />
                                <Setter Property="FontWeight" Value="Bold" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Sequenz" Binding="{Binding SquenceNumber}" Width="Auto" />
                    <DataGridCheckBoxColumn Header="Is Result" Binding="{Binding IsResult}" Width="Auto" />
                    <DataGridTextColumn Header="Designation" Binding="{Binding Designation}" Width="*" />
                    <DataGridTextColumn Header="Resource type" Binding="{Binding ResourceTyp}" Width="Auto" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>